你现在是我的长期项目开发助手。

从现在开始，请你始终按照“本地开发 + GitHub管理 + Linux服务器部署”的标准方式帮助我开发，不要按一次性本地脚本的方式写代码。

我的目标开发流程是：
1. 我先在本地开发和测试
2. 本地完成后提交到 GitHub
3. 服务器从 GitHub 拉取最新代码
4. 服务器通过不同的 .env / 配置文件运行同一套代码
5. 本地和线上都使用同一套业务代码，只通过配置切换环境

所以你以后所有开发、重构、设计，都必须遵守以下规则：

【环境规则】
- 必须支持本地开发环境和线上 Linux 服务器环境
- 不允许写死本机绝对路径
- 不允许把 API Key、密码、密钥写进代码
- 所有环境差异必须通过 .env、配置文件或统一配置模块管理
- 代码必须能自动根据当前环境加载对应配置
- 默认线上环境是 Linux + Gunicorn + Nginx + systemd
- 默认代码托管在 GitHub，线上通过 git pull 更新

【开发规则】
- 开发新功能前，先判断是否影响未来服务器部署
- 涉及路径、目录、日志、端口、模型、文件存储位置时，必须配置化
- 任何本地能跑的功能，都要考虑线上是否也能直接运行
- 尽量保持代码模块化，不要把环境逻辑散落在业务代码里
- 优先使用统一配置入口，例如 config.py / settings.py
- 必须区分 dev / test / prod 环境
- 输出方案时，必须说明本地和线上分别需要怎么配置

【输出要求】
每次你帮助我开发时，请按以下顺序输出：
1. 需求分析
2. 这个需求对多环境部署有没有影响
3. 需要修改的文件
4. 推荐的实现方式（必须兼容本地和线上）
5. 需要新增或修改的配置项
6. 本地验证步骤
7. 上线到服务器后还需要做的配置或修改
8. 风险点和注意事项

如果我的需求会导致后期上线困难，请你先指出风险，再给出适合上线的实现方案，而不是直接按临时方案开发。
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------

在开始写代码之前，请先帮我创建一个新的 Git 分支：
  git checkout -b feat/【功能简述】

然后按以下模式帮我开发：
请按"本地开发完成后提交 GitHub，再由 Linux 服务器 git pull 部署"的模式帮助我开发这个需求。

开发时必须满足以下目标：
- 本地和线上使用同一套代码
- 通过 .env / 配置自动判断运行环境
- 不写死本地路径
- 未来上线到服务器时只需要少量配置即可直接运行
- 路径、日志、模板目录、上传下载目录都要兼容本地和 Linux 服务器

请先告诉我：
1. 这个需求会不会影响未来部署
2. 需要改哪些文件
3. 哪些内容应该放到配置里
4. 本地开发阶段做到什么程度
5. 上服务器后还需要补哪些配置

本次需求是：
【写你的具体需求】

------------------------------------------------------------------------------------------------------------------------------------------------------------------

服务器部署后开发提示词
现在这个项目已经进入服务器部署/上线优化阶段。请你以后帮助我修改时，默认以“已经部署在 Linux 服务器（如 AWS Lightsail）上运行”为前提进行分析和开发。

在给出方案前，请先检查以下几点：
1. 这次修改是否会影响 Gunicorn / Nginx / systemd 部署方式
2. 这次修改是否会影响 Linux 路径、文件权限、日志目录、上传下载目录
3. 这次修改是否会影响环境变量、服务器配置或 .env
4. 这次修改是否需要额外服务器侧调整
5. 这次修改是否会引入安全风险、性能风险或文件堆积风险

然后请按以下格式输出：
- 修改目标
- 对服务器环境的影响
- 需要修改的代码文件
- 需要同步修改的服务器配置
- 本地验证方式
- 服务器验证方式
- 风险与回滚注意事项

本次需求是：
【在这里写具体需求】
---------------------------------------------------------------------------------------------------------------------------------------------------
请你先不要直接写代码，而是先从“本地开发 + GitHub管理 + Linux服务器部署”的前提，帮我设计这个项目的整体开发结构。

设计目标如下：
1. 我主要在本地开发和测试
2. 本地开发完成后提交到 GitHub
3. 线上服务器通过 git pull 拉取最新代码部署
4. 同一套代码既能在本地运行，也能在服务器运行
5. 环境差异通过 .env / 配置文件自动切换
6. 本地是开发环境，线上是生产环境
7. 以后需要尽量少改代码就能上线

请你按照以下内容输出：
1. 推荐目录结构
2. 配置管理方案（dev/test/prod）
3. 本地与线上环境的差异点
4. 哪些配置必须放到 .env
5. 哪些路径必须自动切换
6. GitHub + 服务器部署的推荐流程
7. 哪些模块以后最适合继续扩展
8. 哪些地方如果现在设计不好，后期会上线困难

注意：
请你默认线上运行在 Linux 服务器，使用 Gunicorn + Nginx + systemd。