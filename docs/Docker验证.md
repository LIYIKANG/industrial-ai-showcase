# Docker 验证记录

验证日期：2026-09-17。架构：Linux ARM64。镜像名：`industrial-ai-showcase:1.0.0`。

- 使用 Python 3.12 Debian slim 镜像实际构建成功，`pip check` 通过。
- Compose 启动后容器健康，7 个应用健康检查全部通过。
- 容器运行身份为非 root 的 UID / GID 10001。
- 实际在浏览器内完成 PDF 样例识别、Excel / Word 导出、本地优化求解、配料与 CSV 导出、GPC、MVR 模型切换、四屏交互、原视频播放。
- 验证输入变化时旧结果失效、切换项目时保留当前计算结果。
- 在求解器中保存一条测试项目后强制重建容器，读取到相同保存内容，验证数据卷持久化。
- 修复初始化时加载示例与立即求解之间的并发问题：加载期间暂时禁用相关操作，避免晚到的示例覆盖结果。
- 没有测试生产云端模型计费服务、真实企业微信回调或公网部署。AMD64 尚未进行实际构建验证。

浏览器操作的详细结果见 `runtime/docker-verification.json`；交付包中也提供该记录。

## 1.1.0 Agent 改版

该次镜像为 `industrial-ai-showcase:1.1.0`。功能目录、操作页和交互回归见 `Agent改版与验收.md`。新增浏览器结果为 `runtime/agent-verification.json`。原视频入口已移除，本版本不再以视频播放作为客户流程。原有数据卷保留，业务数据仍需后续脱敏。

## 1.2.0 企业 AI 成熟度诊断

新增第 8 个后端应用，端口 18088；原 18081–18087 对应关系不变。目录现为 7 个可操作 Agent 与 1 个能力方案。新增模块及验收范围见 `企业AI成熟度诊断接入与验收.md`。官网原目录和已有数据卷保留，未迁入联系方式收集或邮件发送流程。
