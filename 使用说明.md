# Agent 体验中心

五个原项目来源和官网的企业 AI 成熟度评估已汇总成一个独立目录，网页按功能组织：7 个可操作 Agent、1 个设备运维能力方案，后台共 8 个应用（包含单据 Word 导出版本）。所有原来源文件夹保持不变。

## 启动

推荐双击 `Docker启动.command`，启动后打开 http://127.0.0.1:18080 。所有应用会同时启动，无需逐个运行。

停止 Docker 版：双击 `Docker停止.command`。数据卷保留，重新启动仍可读取已保存的数据。

没有使用 Docker 时，可双击 `启动演示中心.command`；原生模式端口由启动器分配，以实际打开地址为准。停止原生版使用 `停止全部项目.command`。

## 客户操作顺序

1. 在“能力总览”根据业务功能选择 Agent，点击“查看操作指引”。
2. 阅读业务出发点、产出和准备条件，再按该 Agent 的四步指引体验。
3. 点击“进入操作工作台”，使用真实系统。右侧指引可收起；窄屏时移到系统上方。
4. 每完成一步，手动点击“我已完成，下一步”。标记仅记录阅读／操作进度，不代表系统自动验收结果。
5. 系统中可修改参数、计算和导出。“展开窗口”适合复杂表格；“重新加载”会先提示未提交内容可能丢失。
6. “多屏体验”可同时展示四个系统；每个窗口独立操作。切换 Agent 或窗口中的应用会保留先前页面，整页刷新则会重新加载。

## 功能入口

| Agent | 可以体验 |
| --- | --- |
| 企业 AI 成熟度诊断 Agent | 行业与痛点选择、9 道基础题及追问、评分与雷达图、改善建议、报告下载；虚构示例为 49 分 |
| 经营决策优化 Agent | 加载示例、修改数学模型、求解并解释约束；默认目标值 2360 |
| 单据识别与录入 Agent | PDF 文字层规则提取、人工修正、Excel 导出；扩展版本支持 Word |
| 商品报价校验 Agent | 本地消息输入、字段检查、价格比较和产品库处理 |
| 库存配料优化 Agent | 3,000 kg / 210 Bloom 示例、库存约束求解、CSV 导出 |
| 分子量复配分析 Agent | GPC、黏度法和复配计算；默认 GPC Mw 55,000.00 |
| 能耗与设备诊断 Agent | 模拟工况、模型训练、能耗误差、风险与数据认知 |
| 设备远程运维 Agent | 数据接入、监控、处理流程与试点交付的方案说明；没有运行程序 |

单据两个版本的任务与步骤标记相互独立。先在选择的版本重新载入示例，再进行识别和导出。修改字段后直接下载会先重新校验，避免下载旧结果。

企业成熟度诊断沿用官网本地规则，不调用大模型。点击“载入示例答卷”→“生成诊断报告”可直接体验；正式作答可逐题返回修改。答卷仅在浏览器标签页暂存，刷新可继续，清空时会确认；报告下载为可离线阅读的 HTML，也可通过打印另存 PDF。行业参考值为经验估计，不是真实行业排名。

本地示例无需模型密钥。自由文本 AI 建模或扫描件云端识别需要另外配置对应模型；商品消息未接入真实企业微信，设备诊断未连接 PLC。

当前以电脑上的人工体验为主。平板与手机可访问总览、指引和操作入口；复杂表格建议在较大窗口中展开。

## 数据与资料

“演示样例”仅提供指定 PDF 和模拟能耗数据。原客户附件、方案和录像仍在本地目录，已从网页资料接口移出。

**业务数据尚未完成脱敏**；当前 Docker 镜像和源码仍包含原业务种子及资料，不能因功能改名就视为已匿名化。详细位置和后续方法见 `docs/客户信息脱敏清单.md`。

Docker 使用、架构和离线包见 `Docker部署说明.md`；本轮检查见 `docs/Agent改版与验收.md`。
